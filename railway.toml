[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build && npx drizzle-kit migrate"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
